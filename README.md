# React CV Builder 🚀

Een project om React state management en component architectuur te kneden.

## Roadmap

1. **Fase 1**: Data renderen vanuit `initialData.js` in een mooie UI.
2. **Fase 2**: Formulieren maken die de `cvData` state bijwerken.
3. **Fase 3**: Styling toevoegen (bijv. Tailwind CSS) voor een professionele look.
4. **Extra**: PDF export functionaliteit toevoegen.

## Setup

- `npm install`
- `npm run dev`
