/**
 * Initial Data
 *
 * This file is kept for backward compatibility and potential future use.
 * The application now generates resumes through direct LaTeX generation
 * rather than structured CV data.
 *
 * The CV schema remains available in server/schemas/cvSchema.js for reference.
 */

// Empty export - no longer used in chat-first UI
export const RESUME_SCHEMA_REFERENCE = {
  description: 'Resume schema for documentation. Not used in runtime.'
};
